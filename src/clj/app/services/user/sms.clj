(ns app.services.user.sms
  (:require
    ; [samber.services.http-request :as http-service]
    [clojure.tools.logging :as log]))

; (defn send-sms [params]
;   (http-service/execute :post "http://localhost:3000/api/send" params))
